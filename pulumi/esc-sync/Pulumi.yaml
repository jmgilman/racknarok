name: esc-sync
description: Sync config and secrets to Pulumi ESC environments
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
